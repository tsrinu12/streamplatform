apiVersion: v1
kind: Service
metadata:
  name: {{ include "streamplatform.fullname" . }}
  labels:
    {{- include "streamplatform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "streamplatform.selectorLabels" . | nindent 4 }}
